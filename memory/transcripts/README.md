# Transcripts

Voice-note and meeting transcripts, one file per day: `YYYY-MM-DD.md`, newest content appended under timestamped headings. These are raw capture; the memory-sync workflow extracts decisions, people, dates, and ideas from here into the proper memory files. Keep the raw text intact even after extraction, it's the audit trail for "when did I say that".
