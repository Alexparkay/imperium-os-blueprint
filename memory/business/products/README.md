# Products

One folder per product or productized offer: `<product-name>/` with a `strategy.md` as the living master doc plus whatever research, positioning, and launch material the product accumulates. Each product folder should map to an offer in `context/org/offers.md`; if it doesn't, either register the offer or archive the folder.
