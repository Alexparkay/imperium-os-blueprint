# research/

Research missions that feed content: one dated folder per mission (`YYYY-MM-DD-topic/`) with a `00-`-prefixed master doc first, then supporting material (transcripts, source notes, competitor examples). YouTube research runs through `automations/youtube/` (transcripts via the Apify actor, never page scraping).
